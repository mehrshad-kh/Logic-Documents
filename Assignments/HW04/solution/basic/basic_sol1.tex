\begin{enumerate}
	\item [(الف)]
	زمانی که فعال‌ساز پایین است. چون در \lr{latch} ها زمانی که سیگنال \lr{enable} پایین است، داده‌ها در \lr{latch} ذخیره می‌شوند و ثابت باقی می‌مانند. این به این معناست که \lr{latch} هنگامی که فعال‌ساز پایین است، دیگر تغییرات جدید داده را نمی‌پذیرد و داده‌های موجود در آن ثابت باقی می‌مانند. اگر سیگنال فعال‌ساز بالا باشد، داده‌ها می‌توانند تغییر کنند.
	
	بنابراین، در زمانی که فعال‌ساز پایین است، داده‌ها باید ثابت باقی بمانند و این گزینه صحیح است.
	
	
	
	\item [(ب)]
	گزینه‌های ۱ و ۳
\end{enumerate}