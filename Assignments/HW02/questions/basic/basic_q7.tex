توابع زیر را بدون ساده‌سازی یک بار فقط با استفاده از \lr{NAND}های ۲-ورودی و یک بار فقط با استفاده از \lr{NOR}های ۲-ورودی پیاده‌سازی کنید:

\begin{latin}
	\begin{enumerate}
		\item 
		$ F = A' [B' + C' D' + DE] $
		
		\item 
		$ F = A + BCD' + BCE' + BDE' $
	\end{enumerate}
\end{latin}